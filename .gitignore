__pycache__/
*.pyc
.venv/
venv/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
/frontend/dist/
/frontend/.angular/
*.log
.idea/
.vscode/
.claude/
CLAUDE.local.md
.DS_Store
.env
/backend/outputs/
/backend/logs/
/backend/static/
# Operator profiles written at runtime (carried-over preferences per operator).
# Runtime state, not source — and it names whoever ran the demo.
/backend/data/
# Local test browser for `ng test` (CHROME_BIN), ~150 MB.
/frontend/chrome-headless-shell/

# macOS/Cloud-Sync-Duplikate ("foo 2.ts" neben "foo.ts"). Sie entstehen, wenn
# zwei Rechner dieselbe Datei gleichzeitig schreiben, sind immer ein veralteter
# Schnappschuss der danebenliegenden Datei, und haben `git status` regelmässig
# so vollgeschrieben, dass echte Änderungen darin untergingen.
* 2.*
* 2
